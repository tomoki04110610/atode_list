<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <div class="container">
    <%= link_to "あとでリスト", (logged_in? ? memories_path : login_path), class: "navbar-brand fw-bold" %>

    <% if logged_in? %>
      <div class="d-flex align-items-center">
        <%= link_to "あとでリスト", memories_path, class: "nav-link text-light me-3"%>
        <%= link_to "解決済みリスト", history_memories_path, class: "nav-link text-light me-4" %>
        <span class="navbar-text me-3 text-light border-start ps-3">
          <%= current_user.nickname %>さん
        </span>
        <%= button_to "ログアウト", logout_path, method: :delete, class: "btn btn-outline-light btn-sm" %>
      </div>
    <% end %>
  </div>
</nav>